name: animated_scrollable_timeline
description: A new Flutter package project.
version: 0.0.1
homepage:

environment:
  sdk: ">=3.1.0-255.0.dev <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
